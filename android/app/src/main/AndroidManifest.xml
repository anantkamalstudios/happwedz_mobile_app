<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
      AUDIT FIX: the release manifest declared no INTERNET permission. It only
      worked because the Firebase/Play-Services libraries happen to declare it
      and the merger pulled it in — so the entire app's networking depended on a
      transitive dependency nobody controls. `src/debug` and `src/profile`
      declare it, which is why this never showed up while developing. Declared
      explicitly here so a dependency change can't silently kill every API call
      in a release build. ACCESS_NETWORK_STATE backs `connectivity_plus`.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- FORCE REMOVE ALL FILES ACCESS added by any library -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:node="remove" />

    <!--
      POLICY FIX: Google Play rejects apps targeting API 33+ that declare the
      broad photo/video permissions when system pickers would do. We never
      request these ourselves - the `open_filex` plugin declares them in its own
      manifest and the merger pulled them into the release APK, which is what
      tripped the "Use alternative system pickers for photos / videos" review.
      All picking goes through `image_picker` (system picker, no permission),
      and the only OpenFilex.open() call targets a PDF in the app's private
      documents directory, so nothing here needs media read access. Stripped at
      merge time so a plugin can't reintroduce a policy violation.
    -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_AUDIO"
        tools:node="remove" />

    <application android:label="Happy Wedz"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
            <meta-data android:name="com.google.android.gms.client_id"
                android:value="5404414440-cem4pmibedcbi2qi7h2cgoop841jeot7.apps.googleusercontent.com"
                />
<!--            <meta-data android:name="com.truecaller.android.sdk.ClientId" android:value="gt7crz05tgi0o3mk10wimjig0irao0fr7xgsljqdfh8"/>-->

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data android:name="flutterEmbedding" android:value="2"/>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!--
          Package visibility (Android 11+ / targetSdk 30+): without these,
          canLaunchUrl() returns false and launchUrl() cannot resolve a
          handler, so external links silently do nothing.
        -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <!-- Play Store deep link for the vendor app. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="market"/>
        </intent>
    </queries>
</manifest>